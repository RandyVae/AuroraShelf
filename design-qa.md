# Material 3 Expressive 最终设计审计

日期：2026-09-06  
范围：首页真实列表、连续滚动、搜索、发现、收藏空态、设置、横屏和启动图标。目标设备条件为三星 S26 Ultra 默认字体与显示缩放；截图来自 1080 × 2340、density 440 的 Android 14 模拟器，不冒充 One UI 真机验收。

## 当前证据

按流程保存并逐张打开检查：

1. `captures/ui-audit-2026-09-06/18-release-home-delivery.png` — 最终 release 竖屏首页和真实内容，健康。
2. `captures/ui-audit-2026-09-06/19-nav-final-comparison.png` — 用户截图与最终底栏并排比较；图文距离减半、卡片从面板后方延伸，健康。
3. `captures/ui-audit-2026-09-06/14-release-search-final.png` — 搜索入口与结果列表，健康。
4. `captures/ui-audit-2026-09-06/04-discover.png` — 发现页，健康。
5. `captures/ui-audit-2026-09-06/05-favorites-empty.png` — 收藏空态，健康。
6. `captures/ui-audit-2026-09-06/15-release-settings-final.png` — 站点设置与本地偏好，健康。
7. `captures/ui-audit-2026-09-06/16-release-landscape-final.png` — 横屏自适应导航，健康。
8. `captures/ui-audit-2026-09-06/13-release-launcher-icon-final.png` — 新 adaptive icon 在启动器圆形遮罩下的实际尺寸，健康。
9. `captures/ui-audit-2026-09-06/20-release-large-text-delivery.png` — 1.3 倍系统字体，播放按钮与标题已分离，五项导航完整，健康。

以上九项按流程汇总于 `captures/ui-audit-2026-09-06/21-final-audit-board.png`，便于一次性复核视觉层级和状态一致性。

截图全部来自本轮运行中的真实站点内容；生产包不包含 demo 封面或样例媒体。

## 已修复的问题

- **P1：横屏主内容被底栏压扁。** 横屏从底栏改为 64 dp 侧边导航，推荐卡恢复完整高度；播放按钮移到右上并为标题保留尾部空间。
- **P1：内容透进导航导致文字、图标互相干扰。** 列表视口继续延伸到导航后方，但导航面板保持完全不透明，只在面板四周露出卡片。
- **P2：图标与标签纵向间距过大。** 使用 72 dp 自定义 Material 3 浮动栏，图标容器与标签之间仅保留 1 dp，整体胶囊圆角提高到 36 dp。
- **P2：搜索/发现卡片重复水平边距。** 列表卡片改为由调用方控制外边距，首页和二级页宽度一致，标题获得更多空间。
- **P2：长标题遮挡封面与播放按钮。** 竖屏标题降为 `titleLarge`，两行截断；横屏播放按钮与标题分区。
- **P2：大字号下播放按钮贴近标题。** 竖屏播放按钮上移 24 dp；1.3 倍字体复验不再重叠。
- **P2：分类触控尺寸与状态变化不够明确。** 最小高度提高到 48 dp，选中态使用品牌容器、圆角形变和弹性颜色动效。
- **P1：启动图标被 One UI 遮罩裁切。** 将用户提供的图像前景缩进 20%，使主体落入自适应图标 66dp 安全区。
- **P2：底栏内外曲率不一致。** 外框使用 34dp 半圆角，选中胶囊使用 16dp 半圆角，保持一致的圆弧比例。
- **P2：旧 demo 测试与真实产品行为冲突。** UI 测试改为默认真实站点，不再清空地址进入不存在的 demo 模式。

## 设计判断

界面使用深海军蓝内容背景、近白文字和珊瑚粉单一强调色。首图负责视觉吸引，普通列表保持稳定密度；选中分类和导航使用对比形状，未在每张卡片上堆叠高光、渐变或装饰。竖屏底栏作为浮动层，横屏侧栏作为自适应层，符合 Android 大窗口的空间逻辑。

Material 3 Expressive 在本项目中通过公开 Material 3 组件、主题容器、形状对比和 spring 动效实现，没有依赖内部实验 API，也不再模拟 Apple Liquid Glass。

## 无障碍与证据边界

- 分类和导航项目的可点击区域不小于 48 dp；图标具有中文内容描述，底层页面在搜索或播放器覆盖时移出语义树。
- 1.3 倍系统字体下标题、搜索、分类、列表和五项导航仍可操作；内容标题会截断而不会压缩字体。
- 截图能确认可见布局、层级和截断，不能单独证明完整 TalkBack 阅读顺序、色彩对比数值、120 Hz 流畅度或真机触感反馈。
- 真实 S26 Ultra、One UI、横竖屏播放和长时间滚动仍需用户设备验收。

final result: passed — 当前审计未发现仍待修复的 P0/P1/P2 视觉问题。

# 验证记录 — 2026-09-06

## 已完成

- JDK 21 / Gradle 9.6.0 / AGP 9.4.0，debug 和 R8 release 构建成功。
- 28 项 JVM 单元测试通过，覆盖地址、解析、播放源解码、分页去重、偏好存储、窗口布局、14 个 MISSPipe 服务目录及 Kiosk 映射。
- 模拟器 Android 14 / arm64：9 项设备测试通过；其中 5 项 UI 流程覆盖搜索空态、地址错误/减少透明度、收藏、本地持久化及列表视口穿过浮动导航。
- 实时源站用例：最终复验获取第一页 **36 条**、第二页 **35 条**，并确认第二页包含新增 ID；源站每页数量会变化。检查标题非空、合法页面链接和封面地址，不输出远端标题或地址。
- 实时播放用例：解析第一页视频、建立 Media3 播放器并验证进入 `STATE_READY`，播放进度实际大于 0；测试未记录远端标题或媒体地址。
- 开发测试签名 APK 安装、清空状态冷启动和真实列表显示成功。使用独立包名，保留原 APK 安装。
- 多源检查：MissAV 独立实时列表与封面地址验证通过；其余 MISSPipe 服务验证注册表和分类映射，未逐站执行网络播放，源站可用性仍取决于地区、Cloudflare 与站点维护状态。
- 交付 APK 已通过 v2 签名和 16 KB zipalign 检查，版本为 `118 / 2.6.2`，SHA-256 为 `935d687b1bf901ead23069224bbda704de2fd7d73b87542940b400028098440b`。
- 当前 Material 3 Expressive 审计覆盖首页、滚动、搜索、发现、空态、设置、横屏侧栏、启动图标和 1.3 倍字体；截图保存在 `captures/ui-audit-2026-09-06/`。模拟器字体设置完成后恢复 1.0。

最新设备测试结果位于 `build.nosync/app/outputs/androidTest-results/connected/debug/`；解析/校验测试结果位于 `build.nosync/app/test-results/testDebugUnitTest/`。

## 警告与边界

Android Lint 为 0 errors，保留工具/依赖新版本提示和 target API 提示；没有把依赖更新提示当作自动升级授权。构建还提示一项测试 API 已废弃，以及图形路径 native 库未剥离符号，均未导致编译或安装失败。

以下仍未验收，**不能宣称已经完成原版全部功能迁移或正式发布**：

- 用户真机的声音、连续播放、全屏退出、倍速、旋转、锁屏/切后台与断网恢复；本轮只在模拟器确认真实媒体可解码、进入就绪状态且进度前进。
- Android 8、最新系统、不同厂商设备的运行兼容性和性能。
- TalkBack 实机走查、完整颜色对比度审计、长时间滚动帧率/内存/电量基准。
- 站点全量搜索、登录/会员/支付/评论/下载、原版私有数据迁移；这些模块按需求明确排除。
- 正式发行证书、公开分发许可/第三方 NOTICE 审查、远程仓库与 PR。

## 用户安装检查

1. 从 GitHub Releases 安装对应版本 APK，Android 8.0 或更高；应用名「极光视界」。不需要卸载原版。
2. 首次启动使用已确认的默认站点；首页能否显示列表取决于手机网络与源站当时状态。
3. 验证分类、列表点击、播放、返回、收藏、历史，再检查横屏和后台恢复。
4. 首页使用默认站点加载真实内容；若源站不可达，设置页会显示错误并可重试。
5. 如遇故障，记录手机型号、Android 版本、具体操作和错误提示；不要发送账号密码或敏感内容截图。

这是使用开发证书签名的内部测试候选包，不应作为官方商店发行包。桌面交付文件 SHA-256：`935d687b1bf901ead23069224bbda704de2fd7d73b87542940b400028098440b`。
